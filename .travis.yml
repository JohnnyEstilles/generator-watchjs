language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-watchjs' ]; then cd .. && eval "mv $currentfolder generator-watchjs" && cd generator-watchjs; fi
